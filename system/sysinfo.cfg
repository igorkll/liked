{
    name = "liked",
    versionPath = "/system/version.cfg",
    recoveryMode = true --bootmanager says that it makes sense to set the recoveryMode flag in the unpackBootloader table in the bootloader parameters to start the system in safe mode
}